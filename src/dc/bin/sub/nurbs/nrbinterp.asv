function nrb = nrbinterp(x,y,z)

switch nargin
    case 1,
        ctrlpts = x;
        if size(ctrlpts,1) == 2
            ctrlpts(3,:) = 0;
        end
    case 2
        z = 0*x;
        ctrlpts = [x(:)'; y(:)'; z(:)'];
    case 3
        ctrlpts = [x(:)'; y(:)'; z(:)'];
end


sp = cscvn(ctrlpts);
nrb = pp2nrb(sp);


function nintrp1(c